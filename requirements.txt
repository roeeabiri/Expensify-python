# No external dependencies required.
# The project uses tkinter, which is included in the Python standard library.
# Ensure tkinter is installed on your system (e.g., for Linux: sudo apt-get install python3-tk).
